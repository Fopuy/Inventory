<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pokémon Card Inventory</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <h1>My Pokémon Card Collection</h1>
  <a href="/add" class="add-button">+ Add Card</a>
  <div class="card-grid">
    <% cards.forEach(card => { %>
      <div class="card">
        <div class="image-wrapper">
          <form action="/add/<%= card.id %>/delete" method="POST" class="delete-form">
            <button type="submit" class="delete-button">✖</button>
          </form>
          <img src="<%= card.image_url %>" alt="<%= card.name %>">
        </div>
        <div class="card-details">
          <h4><%= card.name %></h4>
          <p><strong>Rarity:</strong> <%= card.rarity %></p>
          <p><strong>Set:</strong> <%= card.set_name %></p>
          <p><strong>Condition:</strong> <%= card.condition %></p>
          <p><strong>Quantity:</strong> <%= card.quantity %></p>
          <p><strong>Notes:</strong> <%= card.notes %></p>
          <p><strong>Price:</strong> $<%= card.price %></p>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>
